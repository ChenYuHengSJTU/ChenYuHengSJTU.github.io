<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>About Myself</title>
    <url>/2023/02/20/About%20Myself/</url>
    <content><![CDATA[<p>Welcome to MyBlog!</p>
<p>Wish my blog be of some help to you.</p>
<p>Have a nice journey!</p>
]]></content>
  </entry>
  <entry>
    <title>CSAPP_DataLab</title>
    <url>/2023/02/20/CSAPP-DataLab/</url>
    <content><![CDATA[<h1 id="lt-center-gt-CSAPP-Datalab"><a href="#lt-center-gt-CSAPP-Datalab" class="headerlink" title="&lt;center&gt; CSAPP Datalab"></a><code>&lt;center&gt;</code> CSAPP Datalab</h1><p><strong>注意可以使用的op以及op的总个数限制</strong></p>
<h4 id="1"><a href="#1" class="headerlink" title="1"></a>1</h4><ul>
<li>仅使用~和&amp;来实现按位异或</li>
<li>想法来源：离散数学真值表</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * bitXor - x^y using only ~ and &amp; </span><br><span class="line"> *   Example: bitXor(4, 5) = 1</span><br><span class="line"> *   Legal ops: ~ &amp;</span><br><span class="line"> *   Max ops: 14</span><br><span class="line"> *   Rating: 1</span><br><span class="line"> */</span><br><span class="line">int bitXor(int x, int y) &#123;</span><br><span class="line">  int m = (~x) &amp; y;</span><br><span class="line">  int n = (~y) &amp; x;</span><br><span class="line">  return ~((~m) &amp;  (~n));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="2"><a href="#2" class="headerlink" title="2"></a>2</h4><ul>
<li>返回Tmin</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * tmin - return minimum two&#x27;s complement integer </span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 4</span><br><span class="line"> *   Rating: 1</span><br><span class="line"> */</span><br><span class="line">int tmin(void) &#123;</span><br><span class="line">  // easy</span><br><span class="line">  return (1 &lt;&lt; 31);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="3"><a href="#3" class="headerlink" title="3"></a>3</h4><ul>
<li>这题着实把我整崩溃了</li>
<li>其实想法并不难，从Tmax的特性入手即可</li>
<li>0x7fffffff左移1位（即x*2 = x + x ）后加2就是0，当然需要排除-1（0xffffffff）的情况，即!!(x+1)即可</li>
<li>但是在处理的过程中，发现给我的答错了</li>
<li>百思不得其解</li>
<li>后来使用gdb考察汇编，发现整个isTmax函数只有三行汇编，而且汇编的返回值一定是0</li>
<li>解决方法：把makefile中的-Og的优化选项去掉，这样编译器才会乖乖算出(x + x +2)的值,否则就会优化掉</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/*</span><br><span class="line"> * isTmax - returns 1 if x is the maximum, two&#x27;s complement number,</span><br><span class="line"> *     and 0 otherwise </span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | +</span><br><span class="line"> *   Max ops: 10</span><br><span class="line"> *   Rating: 1</span><br><span class="line"> */</span><br><span class="line">int isTmax(int x) &#123;</span><br><span class="line">  int y = x + x + 2;</span><br><span class="line">  return ((!y) &amp; (!!(x + 1)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="4"><a href="#4" class="headerlink" title="4"></a>4</h4><ul>
<li>不断配合使用 &lt;&lt; 和 | ，来构造出0xaaaaaaaa，x &amp; 0xaaaaaaaa后，如果确为allOddbits，应该等于0xaaaaaaaa，在加上 -0xaaaaaaaa （~0xaaaaaaaa+1），判断是否为0</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * allOddBits - return 1 if all odd-numbered bits in word set to 1</span><br><span class="line"> *   where bits are numbered from 0 (least significant) to 31 (most significant)</span><br><span class="line"> *   Examples allOddBits(0xFFFFFFFD) = 0, allOddBits(0xAAAAAAAA) = 1</span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 12</span><br><span class="line"> *   Rating: 2</span><br><span class="line"> */</span><br><span class="line">int allOddBits(int x) &#123;</span><br><span class="line">  // even-numbered bits can also be 1</span><br><span class="line">  int mask1 = 0xaa;</span><br><span class="line">  int mask2 = mask1 &lt;&lt; 8;</span><br><span class="line">  int mask3 = mask2 &lt;&lt; 8;</span><br><span class="line">  int mask4 = mask3 &lt;&lt; 8;</span><br><span class="line">  int mask = (mask1 | mask2 | mask3 | mask4);</span><br><span class="line">  // x &amp; mask clears the impact of even-numbered 1s</span><br><span class="line">  // to judge whether it equals to 0xaaaaaaaa</span><br><span class="line">  return !((x &amp; mask) + (~mask) + 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="5"><a href="#5" class="headerlink" title="5"></a>5</h4><ul>
<li>在树状数组中习得</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * negate - return -x </span><br><span class="line"> *   Example: negate(1) = -1.</span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 5</span><br><span class="line"> *   Rating: 2</span><br><span class="line"> */</span><br><span class="line">int negate(int x) &#123;</span><br><span class="line">  // easy</span><br><span class="line">  return (~x) + 1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="6"><a href="#6" class="headerlink" title="6"></a>6</h4><ul>
<li>第一种方法超出的最多运算符数限制</li>
<li>第二种方法是取出最低位，然后先判断0x30中的3，再分两种情况讨论最低四位，详见注释</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//3</span><br><span class="line">/* </span><br><span class="line"> * isAsciiDigit - return 1 if 0x30 &lt;= x &lt;= 0x39 (ASCII codes for characters &#x27;0&#x27; to &#x27;9&#x27;)</span><br><span class="line"> *   Example: isAsciiDigit(0x35) = 1.</span><br><span class="line"> *            isAsciiDigit(0x3a) = 0.</span><br><span class="line"> *            isAsciiDigit(0x05) = 0.</span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 15</span><br><span class="line"> *   Rating: 3</span><br><span class="line"> */</span><br><span class="line">int isAsciiDigit(int x) &#123;</span><br><span class="line">  // my first method run out of all the 15 available ops</span><br><span class="line"></span><br><span class="line">  // int mask1 = 0xff;</span><br><span class="line">  // int mask2 = mask1 &lt;&lt; 8;</span><br><span class="line">  // int mask3 = mask2 &lt;&lt; 8;</span><br><span class="line">  // int mask4 = mask3 &lt;&lt; 8;</span><br><span class="line">  // int mask = (0xc0 | mask2 | mask3 | mask4);</span><br><span class="line"></span><br><span class="line">  // int c = (x &amp; mask); </span><br><span class="line"></span><br><span class="line">  // int xx = x &amp; 0x3f;</span><br><span class="line"></span><br><span class="line">  // int c1 = !(((xx + (~0x30) +1) &gt;&gt; 31) &amp; (0x1)); // == 0</span><br><span class="line">  // int c2 = !((((~xx) + 1 + 0x39) &gt;&gt; 31) &amp; (0x1)); // == 0</span><br><span class="line"></span><br><span class="line">  // return (!c) &amp; (c1 &amp; c2);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  // first step is to fetch the low four bits</span><br><span class="line">  int mask = 0xf;</span><br><span class="line">  int low = mask &amp; x;</span><br><span class="line">  // then judge the upper 28 bits;use ^ to judge whether it equals to 0x30</span><br><span class="line">  int xx = x + (~low) +1;</span><br><span class="line">  int c = xx ^ 0x30;</span><br><span class="line"></span><br><span class="line">  // two cases of the low four bits</span><br><span class="line">  // 0xxx or 1000 or 1001</span><br><span class="line">  int mid = low &amp; 0x6;</span><br><span class="line">  int high = low &gt;&gt; 3;</span><br><span class="line"></span><br><span class="line">  return (!c) &amp; ((!high) | (!mid));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="7"><a href="#7" class="headerlink" title="7"></a>7</h4><ul>
<li>需要一个小trick：!!x将x转换为0或1</li>
<li>还有就是，得到0或1后，怎么转换为0xffffffff，因为我们要用0xffffffff &amp; y或0xffffffff &amp; z来得到y和z。</li>
<li>还是利用0,1,-1之间的位级关系</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * conditional - same as x ? y : z </span><br><span class="line"> *   Example: conditional(2,4,5) = 4</span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 16</span><br><span class="line"> *   Rating: 3</span><br><span class="line"> */</span><br><span class="line">int conditional(int x, int y, int z) &#123;</span><br><span class="line">  // use ! to judge , to change odinary nums to 0 or 1</span><br><span class="line">  // after we get 0 or 1</span><br><span class="line">  // for 0 : we can use !x to change it to 1 , then ~x + 1 to get -1 , with</span><br><span class="line">  // all bits of 1</span><br><span class="line">  // for 1 : we can use !!x to change it back to 1, then ~x+1 to get -1</span><br><span class="line">  return ((~(!(!x)) + 1) &amp; y) | ((~(!x) + 1) &amp; z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="8"><a href="#8" class="headerlink" title="8"></a>8</h4><ul>
<li>判等：^</li>
<li>基本想法就是作差判断符号</li>
<li>但是需要考虑溢出，仅需考虑正数减负数的溢出</li>
<li>使用符号位判断溢出的特殊情况</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * isLessOrEqual - if x &lt;= y  then return 1, else return 0 </span><br><span class="line"> *   Example: isLessOrEqual(4,5) = 1.</span><br><span class="line"> *   Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 24</span><br><span class="line"> *   Rating: 3</span><br><span class="line"> */</span><br><span class="line">int isLessOrEqual(int x, int y) &#123;</span><br><span class="line">  // use ^ to judge equalance</span><br><span class="line">  // to avoid overflow , use the most significant bit to deal with different cases</span><br><span class="line">  int xs = x &gt;&gt; 31;</span><br><span class="line">  int ys = y &gt;&gt; 31;</span><br><span class="line">  return (!(x ^ y)) | (xs &amp; (!ys)) | ((!(xs ^ ys)) &amp; (x + (~y) +1) &gt;&gt; 31);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="9"><a href="#9" class="headerlink" title="9"></a>9</h4><ul>
<li>实现逻辑非</li>
<li>把数值分为三类：positive，negative，zero</li>
<li>取x与-x的符号位，在进行&amp;0x1（因为对负数实行逻辑右移）+ 1，得到xs与negxs</li>
<li>对于非零值，两者一定有一者为0</li>
<li>对于零，两者均为1</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * logicalNeg - implement the ! operator, using all of </span><br><span class="line"> *              the legal operators except !</span><br><span class="line"> *   Examples: logicalNeg(3) = 0, logicalNeg(0) = 1</span><br><span class="line"> *   Legal ops: ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *   Max ops: 12</span><br><span class="line"> *   Rating: 4 </span><br><span class="line"> */</span><br><span class="line">int logicalNeg(int x) &#123;</span><br><span class="line">  // we divide all x into three groups: 0,positive,negative</span><br><span class="line">  // for negative and positive,one and only one of the significant bit of x and -x will be 1</span><br><span class="line">  // but for 0,they are all 0</span><br><span class="line">  // so we add 1 ,keep the trait for positive and negative numbers,but change the case of 0</span><br><span class="line">  // last,for non-zero : xs == 0 &amp;&amp; negxs == 1 || xs == 1 &amp;&amp; negxs == 0</span><br><span class="line">  // for zero : xs == 1 || negxs == 1 </span><br><span class="line">  int xs = (((x &gt;&gt; 31) &amp; 0x1) + 1) &amp; 0x1 ;</span><br><span class="line">  int negxs = (((((~x) + 1) &gt;&gt; 31) &amp; 0x1) + 1) &amp; 0x1 ;</span><br><span class="line">  return xs &amp; negxs;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="10"><a href="#10" class="headerlink" title="10"></a>10</h4><ul>
<li>kind of hard</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* howManyBits - return the minimum number of bits required to represent x in</span><br><span class="line"> *             two&#x27;s complement</span><br><span class="line"> *  Examples: howManyBits(12) = 5</span><br><span class="line"> *            howManyBits(298) = 10</span><br><span class="line"> *            howManyBits(-5) = 4</span><br><span class="line"> *            howManyBits(0)  = 1</span><br><span class="line"> *            howManyBits(-1) = 1</span><br><span class="line"> *            howManyBits(0x80000000) = 32</span><br><span class="line"> *  Legal ops: ! ~ &amp; ^ | + &lt;&lt; &gt;&gt;</span><br><span class="line"> *  Max ops: 90</span><br><span class="line"> *  Rating: 4</span><br><span class="line"> */</span><br><span class="line">int howManyBits(int x) &#123;</span><br><span class="line"></span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="11"><a href="#11" class="headerlink" title="11"></a>11</h4><ul>
<li>浮点数的三道题均不难，更多的是考察杜宇IEEE754浮点数标准的理解记忆</li>
<li>第一题，分情况（规格化，非规格化，NAN）讨论即可</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * floatScale2 - Return bit-level equivalent of expression 2*f for</span><br><span class="line"> *   floating point argument f.</span><br><span class="line"> *   Both the argument and result are passed as unsigned int&#x27;s, but</span><br><span class="line"> *   they are to be interpreted as the bit-level representation of</span><br><span class="line"> *   single-precision floating point values.</span><br><span class="line"> *   When argument is NaN, return argument</span><br><span class="line"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. also if, while</span><br><span class="line"> *   Max ops: 30</span><br><span class="line"> *   Rating: 4</span><br><span class="line"> */</span><br><span class="line">unsigned floatScale2(unsigned uf) &#123;</span><br><span class="line">  int expmask = 0xff &lt;&lt; 23;</span><br><span class="line">  int s = uf &gt;&gt; 31;</span><br><span class="line">  int fmask1 = 0xff;</span><br><span class="line">  int fmask2 = fmask1 &lt;&lt; 8;</span><br><span class="line">  int fmask3 = fmask2 &lt;&lt; 7;</span><br><span class="line">  int fmask = fmask1 | fmask2 | fmask3;</span><br><span class="line">  int frac = uf &amp; fmask;</span><br><span class="line">  int exp = (uf &amp; expmask) &gt;&gt; 23;</span><br><span class="line">  if(exp &amp;&amp; (exp - 0xff))</span><br><span class="line">    exp = exp + 1;</span><br><span class="line">  if(!exp)</span><br><span class="line">    frac = frac &lt;&lt; 1;</span><br><span class="line">  uf = (s &lt;&lt; 31) | (exp &lt;&lt; 23) | (frac);</span><br><span class="line">  return uf;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="12"><a href="#12" class="headerlink" title="12"></a>12</h4><ul>
<li>也是只需分情况讨论即可，注意exp位域的最大值为1111110，不能是1111111</li>
<li>从这道题中可以get到，对于溢出，均处理为inf，而不是NAN</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * floatFloat2Int - Return bit-level equivalent of expression (int) f</span><br><span class="line"> *   for floating point argument f.</span><br><span class="line"> *   Argument is passed as unsigned int, but</span><br><span class="line"> *   it is to be interpreted as the bit-level representation of a</span><br><span class="line"> *   single-precision floating point value.</span><br><span class="line"> *   Anything out of range (including NaN and infinity) should return</span><br><span class="line"> *   0x80000000u.</span><br><span class="line"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. also if, while</span><br><span class="line"> *   Max ops: 30</span><br><span class="line"> *   Rating: 4</span><br><span class="line"> */</span><br><span class="line">int floatFloat2Int(unsigned uf) &#123;</span><br><span class="line">  int expmask = 0xff &lt;&lt; 23;</span><br><span class="line">  int exp = (uf &amp; expmask) &gt;&gt; 23;</span><br><span class="line">  </span><br><span class="line">  int s = uf &gt;&gt; 31;</span><br><span class="line">  </span><br><span class="line">  int fmask1 = 0xff;</span><br><span class="line">  int fmask2 = fmask1 &lt;&lt; 8;</span><br><span class="line">  int fmask3 = fmask2 &lt;&lt; 7;</span><br><span class="line">  int fmask = fmask1 | fmask2 | fmask3;</span><br><span class="line">  int frac = uf &amp; fmask;</span><br><span class="line"></span><br><span class="line">  int M;</span><br><span class="line">  int E;</span><br><span class="line">  int bias = 127;</span><br><span class="line"></span><br><span class="line">  if(exp == 0xff)&#123;</span><br><span class="line">    return 0x80000000u;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  if(exp &amp;&amp; (exp - 0xff))&#123;</span><br><span class="line">    M = 1 &lt;&lt; 23 | frac;</span><br><span class="line">    E = exp - bias;</span><br><span class="line">  &#125;</span><br><span class="line">  else&#123;</span><br><span class="line">    M = frac;</span><br><span class="line">    E = 1 - bias;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  int res;</span><br><span class="line">  if(E &gt; 23)&#123;</span><br><span class="line">    res = M &lt;&lt; E;</span><br><span class="line">    // do not forget this anything out of range</span><br><span class="line">    if(E &gt;= 8) return 0x80000000u;</span><br><span class="line">  &#125;</span><br><span class="line">  else if(E == 23) res = M;</span><br><span class="line">  else&#123;</span><br><span class="line">    int e = 23 - E;</span><br><span class="line">    if(e &gt;= 24) return 0;</span><br><span class="line">    res = M &gt;&gt; (23 - E);</span><br><span class="line">  &#125;</span><br><span class="line">  if(s) return -res;</span><br><span class="line">  return res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="13"><a href="#13" class="headerlink" title="13"></a>13</h4><ul>
<li>特别注意float的表示范围，是不对称的，既要考虑规格化，也要考虑非规格化</li>
<li>具体细节可以看书</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* </span><br><span class="line"> * floatPower2 - Return bit-level equivalent of the expression 2.0^x</span><br><span class="line"> *   (2.0 raised to the power x) for any 32-bit integer x.</span><br><span class="line"> *</span><br><span class="line"> *   The unsigned value that is returned should have the identical bit</span><br><span class="line"> *   representation as the single-precision floating-point number 2.0^x.</span><br><span class="line"> *   If the result is too small to be represented as a denorm, return</span><br><span class="line"> *   0. If too large, return +INF.</span><br><span class="line"> * </span><br><span class="line"> *   Legal ops: Any integer/unsigned operations incl. ||, &amp;&amp;. Also if, while </span><br><span class="line"> *   Max ops: 30 </span><br><span class="line"> *   Rating: 4</span><br><span class="line"> */</span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">unsigned floatPower2(int x) &#123;</span><br><span class="line">  int exp;</span><br><span class="line">  int frac;</span><br><span class="line">  int s = 0;</span><br><span class="line">  int res = 0;</span><br><span class="line"></span><br><span class="line">  if(x &gt;= 128)&#123;</span><br><span class="line">    exp = 0xff;</span><br><span class="line">    frac = 0;</span><br><span class="line">    s = 0;</span><br><span class="line">  &#125;</span><br><span class="line">  else if(x &lt; -126 - 23)&#123;</span><br><span class="line">    frac = 0;</span><br><span class="line">    exp = 0;</span><br><span class="line">  &#125;</span><br><span class="line">  else if(x &gt;= -126 - 23 &amp;&amp; x &lt;= -127)&#123;</span><br><span class="line">    exp = 0;</span><br><span class="line">    frac = 1 &lt;&lt; (x - (-126 - 23));</span><br><span class="line">  &#125;</span><br><span class="line">  else &#123;</span><br><span class="line">    frac = 0;</span><br><span class="line">    exp = 127 + x;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  res = (exp &lt;&lt; 23) | (frac) | (s &lt;&lt; 31);</span><br><span class="line"></span><br><span class="line">  return res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>CSAPP</category>
      </categories>
      <tags>
        <tag>Lab</tag>
      </tags>
  </entry>
</search>
